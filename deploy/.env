# JGameServer local Docker Compose defaults.
# start-server.bat passes this file explicitly with --env-file.

NODE_IMAGE=eclipse-temurin:8-jre
# 实测 Spring Boot 2.0.2 (mongodb-driver 3.6.3) 可直连 mongo:7 standalone
MONGO_IMAGE=mongo:7.0
REDIS_IMAGE=redis:6.2-alpine

MONGO_URI=mongodb://mongo:27017/jgame_server
