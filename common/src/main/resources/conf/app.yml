# 业务公共配置（原各服务 config.properties / propertyConfig.xml 归并）
# 心跳（秒）：读空闲/写空闲/全空闲，超过 all 客户端无发包判定空闲
socketReaderIdleTime: 0
socketWriterIdleTime: 0
socketAllIdleTime: 300
# 各业务服务默认 id（net.yml 有 portOffset 时同机多节点可再偏移）
lobbyId: 1
battleServerId: 1
globalId: 1
